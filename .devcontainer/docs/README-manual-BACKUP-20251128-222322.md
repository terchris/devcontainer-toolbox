# DevContainer Additions Manual

> **Auto-generated documentation for all installation scripts**  
> Last updated: 2025-11-28 21:21:57  
> Generated by: `.devcontainer/manage/generate-manual.sh`

This manual provides comprehensive documentation for all available installation scripts in the devcontainer additions.

## Categories

This manual is organized into the following categories:

### Development Tools

Programming language development environments and tools (Python, TypeScript, Go, Rust, C#, Java, PHP)

**Scripts in this category:**        7

### AI & Machine Learning Tools

AI and machine learning development tools (Claude Code, etc.)

**Scripts in this category:**        1

### Cloud & Infrastructure Tools

Cloud platform tools and SDKs (Azure, AWS, GCP)

**Scripts in this category:**        1

### Data & Analytics Tools

Data analysis, visualization, and data engineering tools (Jupyter, pandas, DBT)

**Scripts in this category:**        1

### Infrastructure & Configuration

Infrastructure as Code, configuration management, and DevOps tools (Ansible, Kubernetes, Terraform)

**Scripts in this category:**        5

---## Table of Contents

- [Development Tools](#development-tools)
- [AI & Machine Learning Tools](#ai---machine-learning-tools)
- [Cloud & Infrastructure Tools](#cloud---infrastructure-tools)
- [Data & Analytics Tools](#data---analytics-tools)
- [Infrastructure & Configuration](#infrastructure---configuration)

---[21:21:57] ℹ️    Generating section: Development Tools
[21:21:57] ℹ️      Processed 7 scripts in category
## Development Tools

### C# Development Tools

**Script ID:** `dev-csharp`  
**Script:** `install-dev-csharp.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-csharp-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-csharp
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-csharp
Name:         install-dev-csharp
Category:     LANGUAGE_DEV
Description:  Complete .NET 8.0 development environment with Azure Functions, Bicep IaC, storage emulation, and VS Code extensions

Check Command: [ -f /Users/terje.christensen/.dotnet/dotnet ] || [ -f /usr/bin/dotnet ] || command -v dotnet >/dev/null 2>&1

Usage:
  install-dev-csharp.sh              # Install C# development environment
  install-dev-csharp.sh --help       # Show this help
  install-dev-csharp.sh --uninstall  # Uninstall C# and .NET tools
  install-dev-csharp.sh --debug      # Install with debug output

Node.js Packages (NPM):
  - azurite

Verification Commands:
  - if command -v dotnet >/dev/null; then echo "✅ .NET SDK: $(dotnet --version)"; else echo "❌ .NET SDK not found"; fi
  - if dotnet --list-sdks | grep -q "8.0"; then echo "✅ .NET SDK 8.0 is installed"; else echo "❌ .NET SDK 8.0 not found"; fi
  - if command -v func >/dev/null; then echo "✅ Azure Functions Core Tools: $(func --version)"; else echo "❌ Azure Functions Core Tools not found"; fi
  - if code --list-extensions | grep -q ms-dotnettools.csdevkit; then echo "✅ C# Dev Kit is installed"; else echo "❌ C# Dev Kit not installed"; fi

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Go Runtime & Development Tools

**Script ID:** `dev-golang`  
**Script:** `install-dev-golang.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-golang-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-golang
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-golang
Name:         install-dev-golang
Category:     LANGUAGE_DEV
Description:  Installs Go runtime, common tools, and VS Code extensions for Go development.

Check Command: [ -f /usr/local/go/bin/go ] || [ -f /usr/bin/go ] || command -v go >/dev/null 2>&1

Usage:
  install-dev-golang.sh                    # Install Go (default version)
  install-dev-golang.sh --help             # Show this help
  install-dev-golang.sh --version 1.21.0   # Install specific Go version
  install-dev-golang.sh --uninstall        # Uninstall Go
  install-dev-golang.sh --debug            # Install with debug output

System Packages (APT):
  - curl
  - gnupg
  - apt-transport-https
  - ca-certificates

Go Packages (go install):
  - golang.org/x/tools/gopls@latest
  - github.com/go-delve/delve/cmd/dlv@latest
  - honnef.co/go/tools/cmd/staticcheck@latest

Verification Commands:
  - command -v go >/dev/null && go version || echo '❌ Go not found'
  - go env || echo '❌ Failed to get Go environment'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Java Runtime & Development Tools

**Script ID:** `dev-java`  
**Script:** `install-dev-java.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-java-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-java
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-java
Name:         install-dev-java
Category:     LANGUAGE_DEV
Description:  Installs Java JDK, Maven, Gradle, and VS Code extensions for Java development.

Check Command: [ -f /usr/bin/java ] || [ -f /usr/lib/jvm/*/bin/java ] || command -v java >/dev/null 2>&1

Usage:
  install-dev-java.sh                  # Install Java (default version)
  install-dev-java.sh --help           # Show this help
  install-dev-java.sh --version 17     # Install specific Java version
  install-dev-java.sh --uninstall      # Uninstall Java
  install-dev-java.sh --debug          # Install with debug output

System Packages (APT):
  - curl
  - gnupg
  - apt-transport-https
  - ca-certificates
  - wget

Verification Commands:
  - command -v java >/dev/null && java -version || echo '❌ Java not found'
  - command -v javac >/dev/null && javac -version || echo '❌ Java compiler not found'
  - command -v mvn >/dev/null && mvn --version || echo '❌ Maven not found'
  - command -v gradle >/dev/null && gradle --version || echo '❌ Gradle not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### PHP Laravel Development Tools

**Script ID:** `dev-php-laravel`  
**Script:** `install-dev-php-laravel.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-php-laravel-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-php-laravel
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-php-laravel
Name:         install-dev-php-laravel
Category:     LANGUAGE_DEV
Description:  Installs PHP 8.4, Composer, Laravel installer, and sets up Laravel development environment

Check Command: ([ -f /usr/bin/php ] || command -v php >/dev/null 2>&1) && ([ -f /usr/local/bin/composer ] || command -v composer >/dev/null 2>&1) && ([ -f /Users/terje.christensen/.composer/vendor/bin/laravel ] || command -v laravel >/dev/null 2>&1)

Usage:
  install-dev-php-laravel.sh              # Install PHP Laravel development environment
  install-dev-php-laravel.sh --help       # Show this help
  install-dev-php-laravel.sh --uninstall  # Uninstall PHP and Laravel tools
  install-dev-php-laravel.sh --debug      # Install with debug output

Packages: Custom installation (see script for details)

Verification Commands:
  - command -v php >/dev/null && php --version | head -n 1 || echo '❌ PHP not found'
  - command -v composer >/dev/null && composer --version | head -n 1 || echo '❌ Composer not found'
  - command -v laravel >/dev/null && laravel --version || echo '❌ Laravel installer not found'
  - php -m | grep -q 'mbstring' && echo '✅ PHP mbstring extension loaded' || echo '❌ PHP mbstring extension missing'
  - php -m | grep -q 'curl' && echo '✅ PHP curl extension loaded' || echo '❌ PHP curl extension missing'
  - php -m | grep -q 'sqlite3' && echo '✅ PHP SQLite extension loaded' || echo '❌ PHP SQLite extension missing'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Python Development Tools

**Script ID:** `dev-python`  
**Script:** `install-dev-python.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-python-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-python
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-python
Name:         install-dev-python
Category:     LANGUAGE_DEV
Description:  Installs Python 3.11+, pip, venv, and essential development tools

Check Command: [ -f /usr/local/bin/python3 ] || [ -f /usr/bin/python3 ] || command -v python3 >/dev/null 2>&1

Usage:
  install-dev-python.sh              # Install Python development environment
  install-dev-python.sh --help       # Show this help
  install-dev-python.sh --uninstall  # Uninstall Python packages (system Python remains)
  install-dev-python.sh --debug      # Install with debug output

System Packages (APT):
  - python3
  - python3-pip
  - python3-venv
  - python3-dev
  - python3-setuptools
  - python3-wheel
  - build-essential
  - libffi-dev
  - libssl-dev

Python Packages (pip):
  - pip
  - setuptools
  - wheel
  - virtualenv
  - requests
  - pytest
  - black
  - flake8
  - mypy

Verification Commands:
  - command -v python3 >/dev/null && python3 --version || echo '❌ Python not found'
  - command -v pip3 >/dev/null && pip3 --version || echo '❌ pip not found'
  - python3 -c 'import venv' 2>/dev/null && echo '✅ venv module available' || echo '❌ venv module not available'
  - grep -q 'alias python=' ~/.bashrc && echo '✅ Python aliases configured' || echo 'ℹ️  Python aliases not yet active (restart shell)'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Rust Development Tools

**Script ID:** `dev-rust`  
**Script:** `install-dev-rust.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-rust-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-rust
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-rust
Name:         install-dev-rust
Category:     LANGUAGE_DEV
Description:  Installs Rust (latest stable via rustup), cargo, and sets up Rust development environment

Check Command: [ -f /Users/terje.christensen/.cargo/bin/rustc ] || command -v rustc >/dev/null 2>&1

Usage:
  install-dev-rust.sh              # Install Rust development environment
  install-dev-rust.sh --help       # Show this help
  install-dev-rust.sh --uninstall  # Uninstall Rust and cargo tools
  install-dev-rust.sh --debug      # Install with debug output

System Packages (APT):
  - build-essential
  - pkg-config
  - libssl-dev

Rust Packages (cargo install):
  - cargo-edit
  - cargo-watch
  - cargo-outdated

Verification Commands:
  - command -v rustc >/dev/null && echo '✅ Rust compiler is available' || echo '❌ Rust compiler not found'
  - command -v cargo >/dev/null && echo '✅ Cargo is available' || echo '❌ Cargo not found'
  - command -v rustup >/dev/null && echo '✅ Rustup is available' || echo '❌ Rustup not found'
  - test -d $HOME/.cargo && echo '✅ Cargo directory exists' || echo '❌ Cargo directory not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### TypeScript Development Tools

**Script ID:** `dev-typescript`  
**Script:** `install-dev-typescript.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-typescript-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-typescript
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-typescript
Name:         install-dev-typescript
Category:     LANGUAGE_DEV
Description:  Installs Node.js LTS, npm, TypeScript, and essential development tools

Check Command: command -v tsc >/dev/null 2>&1 || (test -f ~/.npm-global/bin/tsc || npm list -g --depth=0 2>/dev/null | grep -q typescript)

Usage:
  install-dev-typescript.sh              # Install TypeScript development environment
  install-dev-typescript.sh --help       # Show this help
  install-dev-typescript.sh --uninstall  # Uninstall TypeScript and Node packages
  install-dev-typescript.sh --debug      # Install with debug output

System Packages (APT):
  - curl
  - ca-certificates
  - gnupg
  - lsb-release
  - build-essential
  - git

Node.js Packages (NPM):
  - typescript
  - tsx
  - @types/node
  - nodemon
  - ts-node
  - eslint
  - prettier
  - @typescript-eslint/parser
  - @typescript-eslint/eslint-plugin
  - jest
  - @types/jest
  - ts-jest

Verification Commands:
  - command -v node >/dev/null && node --version || echo '❌ Node.js not found'
  - command -v npm >/dev/null && npm --version || echo '❌ npm not found'
  - command -v tsc >/dev/null && tsc --version || echo '❌ TypeScript not found'
  - command -v tsx >/dev/null && tsx --version || echo '❌ tsx not found'
  - test -d $HOME/.npm-global && echo '✅ Global npm directory configured' || echo 'ℹ️  Global npm directory not configured'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[21:21:57] ℹ️    Generating section: AI & Machine Learning Tools
[21:21:57] ℹ️      Processed 1 scripts in category
## AI & Machine Learning Tools

### Claude Code

**Script ID:** `dev-ai-claudecode`  
**Script:** `install-dev-ai-claudecode.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-ai-claudecode-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-ai-claudecode
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-ai-claudecode
Name:         install-dev-ai-claudecode
Category:     AI_TOOLS
Description:  Installs Claude Code, Anthropic's terminal-based AI coding assistant with agentic capabilities and LSP integration

Check Command: [ -f /Users/terje.christensen/.local/bin/claude ] || [ -f /usr/local/bin/claude ] || command -v claude >/dev/null 2>&1

Usage:
  install-dev-ai-claudecode.sh              # Install Claude Code
  install-dev-ai-claudecode.sh --help       # Show this help
  install-dev-ai-claudecode.sh --uninstall  # Uninstall Claude Code
  install-dev-ai-claudecode.sh --debug      # Install with debug output

System Packages (APT):
  - curl

Node.js Packages (NPM):
  - @anthropic-ai/claude-code

Verification Commands:
  - command -v claude >/dev/null && echo '✅ Claude Code binary is available' || echo '❌ Claude Code binary not found'
  - test -L /home/vscode/.claude-code-env && echo '✅ Environment config symlink exists' || echo '⚠️  Environment config symlink not found'
  - test -d /workspace/.devcontainer.secrets/env-vars && echo '✅ Environment directory exists in .devcontainer.secrets/' || echo '❌ Environment directory not found'
  - test -d /workspace/.claude/skills && echo '✅ Skills directory exists' || echo '⚠️  Skills directory not found'
  - grep -q '.devcontainer.secrets/' /workspace/.gitignore && echo '✅ .devcontainer.secrets/ is gitignored' || echo '❌ .devcontainer.secrets/ NOT gitignored (SECURITY RISK!)'
  - grep -q 'Claude Code environment' /home/vscode/.bashrc && echo '✅ Environment loading added to bashrc' || echo '⚠️  bashrc not configured'
  - claude --version >/dev/null 2>&1 && echo '✅ Claude Code is functional' || echo '⚠️  Claude Code installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[21:21:57] ℹ️    Generating section: Cloud & Infrastructure Tools
[21:21:57] ℹ️      Processed 1 scripts in category
## Cloud & Infrastructure Tools

### Azure Development Tools

**Script ID:** `tool-azure`  
**Script:** `install-tool-azure.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-azure-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-tool-azure
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-azure
Name:         install-tool-azure
Category:     CLOUD_TOOLS
Description:  Installs Azure CLI and Azure VS Code extensions for comprehensive Azure cloud development

Check Command: [ -f /usr/bin/az ] || [ -f /usr/local/bin/az ] || command -v az >/dev/null 2>&1

Usage:
  install-tool-azure.sh              # Install Azure development tools
  install-tool-azure.sh --help       # Show this help
  install-tool-azure.sh --uninstall  # Uninstall Azure tools
  install-tool-azure.sh --debug      # Install with debug output

Packages: Custom installation (see script for details)

Verification Commands:
  - command -v az >/dev/null && az version --output json | head -10 || echo '❌ Azure CLI not found'
  - az account list-locations --output table --query '[0:3]' 2>/dev/null || echo 'ℹ️  Not logged in to Azure (run: az login)'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[21:21:57] ℹ️    Generating section: Data & Analytics Tools
[21:21:57] ℹ️      Processed 1 scripts in category
## Data & Analytics Tools

### Data & Analytics Tools

**Script ID:** `tool-dataanalytics`  
**Script:** `install-tool-dataanalytics.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-dataanalytics-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-tool-dataanalytics
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-dataanalytics
Name:         install-tool-dataanalytics
Category:     DATA_ANALYTICS
Description:  Installs Python data analysis libraries, Jupyter notebooks, and related VS Code extensions

Check Command: [ -f /usr/local/bin/jupyter ] || [ -f /Users/terje.christensen/.local/bin/jupyter ] || command -v jupyter >/dev/null 2>&1

Usage:
  install-tool-dataanalytics.sh              # Install data analytics tools
  install-tool-dataanalytics.sh --help       # Show this help
  install-tool-dataanalytics.sh --uninstall  # Uninstall analytics tools
  install-tool-dataanalytics.sh --debug      # Install with debug output

Python Packages (pip):
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - scikit-learn
  - jupyter
  - jupyterlab
  - notebook
  - dbt-core
  - dbt-postgres

Verification Commands:
  - python --version || echo '❌ Python not found'
  - pip --version || echo '❌ pip not found'
  - python -c 'import pandas' 2>/dev/null && echo '✅ pandas is installed' || echo '❌ pandas not found'
  - python -c 'import numpy' 2>/dev/null && echo '✅ numpy is installed' || echo '❌ numpy not found'
  - python -c 'import matplotlib' 2>/dev/null && echo '✅ matplotlib is installed' || echo '❌ matplotlib not found'
  - python -c 'import seaborn' 2>/dev/null && echo '✅ seaborn is installed' || echo '❌ seaborn not found'
  - python -c 'import sklearn' 2>/dev/null && echo '✅ scikit-learn is installed' || echo '❌ scikit-learn not found'
  - jupyter --version >/dev/null 2>&1 && echo '✅ jupyter is installed' || echo '❌ jupyter not found'
  - dbt --version >/dev/null 2>&1 && echo '✅ dbt is installed' || echo '❌ dbt not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---[21:21:57] ℹ️    Generating section: Infrastructure & Configuration
[21:21:58] ℹ️      Processed 5 scripts in category
## Infrastructure & Configuration

### PowerShell

**Script ID:** `dev-powershell`  
**Script:** `install-dev-powershell.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-dev-powershell-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-dev-powershell
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           dev-powershell
Name:         install-dev-powershell
Category:     INFRA_CONFIG
Description:  Installs PowerShell runtime and modules for Azure and Microsoft Graph development

Check Command: [ -f /usr/bin/pwsh ] || [ -f /opt/microsoft/powershell/7/pwsh ] || command -v pwsh >/dev/null 2>&1

Usage:
  install-dev-powershell.sh              # Install PowerShell environment
  install-dev-powershell.sh --help       # Show this help
  install-dev-powershell.sh --uninstall  # Uninstall PowerShell
  install-dev-powershell.sh --debug      # Install with debug output

PowerShell Modules:
  - Az
  - Microsoft.Graph
  - PSScriptAnalyzer

Verification Commands:
  - command -v pwsh >/dev/null && pwsh -Version || echo '❌ PowerShell not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable Az* | Select-Object Name, Version" || echo '❌ Az module not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable Microsoft.Graph | Select-Object Name, Version" || echo '❌ Microsoft.Graph module not found'
  - pwsh -NoProfile -NonInteractive -Command "Get-Module -ListAvailable PSScriptAnalyzer | Select-Object Name, Version" || echo '❌ PSScriptAnalyzer not found'
  - code --list-extensions | grep -q ms-vscode.powershell && echo '✅ PowerShell extension is installed' || echo '❌ PowerShell extension is not installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Nginx Reverse Proxy

**Script ID:** `srv-nginx`  
**Script:** `install-srv-nginx.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-srv-nginx-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-srv-nginx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           srv-nginx
Name:         install-srv-nginx
Category:     INFRA_CONFIG
Description:  Install nginx as reverse proxy for Claude Code → LiteLLM with Host header injection

Check Command: command -v nginx >/dev/null 2>&1

Usage:
  install-srv-nginx.sh              # Install nginx reverse proxy
  install-srv-nginx.sh --help       # Show this help
  install-srv-nginx.sh --uninstall  # Uninstall nginx
  install-srv-nginx.sh --debug      # Install with debug output

System Packages (APT):
  - nginx-light

Verification Commands:
  - nginx -v 2>&1 | head -1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### OTel Collector

**Script ID:** `srv-otel`  
**Script:** `install-srv-otel-monitoring.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-srv-otel-monitoring-20251128-212157.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-srv-otel-monitoring
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           srv-otel
Name:         install-srv-otel-monitoring
Category:     INFRA_CONFIG
Description:  Install OpenTelemetry Collector for devcontainer monitoring when connected to our network

Check Command: ([ -f /usr/bin/otelcol-contrib ] || command -v otelcol-contrib >/dev/null 2>&1) && ([ -f /usr/local/bin/script_exporter ] || command -v script_exporter >/dev/null 2>&1)

Prerequisites:
  - config-devcontainer-identity.sh config-nginx.sh

Usage:
  install-srv-otel-monitoring.sh              # Install OpenTelemetry Collector
  install-srv-otel-monitoring.sh --help       # Show this help
  install-srv-otel-monitoring.sh --uninstall  # Uninstall OTel monitoring
  install-srv-otel-monitoring.sh --debug      # Install with debug output

Packages: Custom installation (see script for details)

Verification Commands:
  - command -v otelcol-contrib >/dev/null && echo '✅ otelcol-contrib binary found in PATH' || echo '❌ otelcol-contrib not found'
  - otelcol-contrib --version 2>&1 | head -1
  - command -v script_exporter >/dev/null && echo '✅ script_exporter binary found in PATH' || echo '❌ script_exporter not found'
  - script_exporter --version 2>&1 | head -1
  - test -d '/workspace/.devcontainer/additions/otel' && echo '✅ Config directory exists' || echo '❌ Config directory not found'
  - test -f '/workspace/.devcontainer/additions/otel/script-exporter-config.yaml' && echo '✅ script_exporter config exists' || echo '⚠️  script_exporter config not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Configuration Tools

**Script ID:** `tool-iac`  
**Script:** `install-tool-iac.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-iac-20251128-212158.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-tool-iac
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-iac
Name:         install-tool-iac
Category:     INFRA_CONFIG
Description:  Installs tools and extensions for Infrastructure as Code (IaC) and configuration management (Ansible)

Check Command: [ -f /usr/local/bin/ansible ] || [ -f /usr/bin/ansible ] || command -v ansible >/dev/null 2>&1

Usage:
  install-tool-iac.sh              # Install IaC tools
  install-tool-iac.sh --help       # Show this help
  install-tool-iac.sh --uninstall  # Uninstall IaC tools
  install-tool-iac.sh --debug      # Install with debug output

System Packages (APT):
  - ansible
  - ansible-lint

Verification Commands:
  - command -v ansible >/dev/null && ansible --version | head -n1 || echo '❌ ansible not found'
  - command -v ansible-lint >/dev/null && ansible-lint --version || echo '❌ ansible-lint not found'
  - code --list-extensions | grep -q redhat.ansible && echo '✅ Ansible extension is installed' || echo '❌ Ansible extension is not installed'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---

### Kubernetes Development Tools

**Script ID:** `tool-kubernetes`  
**Script:** `install-tool-kubernetes.sh`

```
📝 Logging to: /tmp/devcontainer-install/install-tool-kubernetes-20251128-212158.log
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 install-tool-kubernetes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:           tool-kubernetes
Name:         install-tool-kubernetes
Category:     INFRA_CONFIG
Description:  Installs kubectl, k9s, helm and sets up .devcontainer.secrets folder for kubeconfig

Check Command: command -v kubectl >/dev/null 2>&1 || command -v k9s >/dev/null 2>&1 || command -v helm >/dev/null 2>&1

Usage:
  install-tool-kubernetes.sh              # Install Kubernetes development tools
  install-tool-kubernetes.sh --help       # Show this help
  install-tool-kubernetes.sh --uninstall  # Uninstall Kubernetes tools
  install-tool-kubernetes.sh --debug      # Install with debug output

System Packages (APT):
  - curl
  - ca-certificates

Verification Commands:
  - command -v kubectl >/dev/null && kubectl version --client || echo '❌ kubectl not found'
  - command -v k9s >/dev/null && k9s version -s || echo '❌ k9s not found'
  - command -v helm >/dev/null && helm version --short || echo '❌ helm not found'
  - test -f /workspace/.devcontainer.secrets/.kube/config && echo '✅ kubeconfig found' || echo '⚠️  kubeconfig not found'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
---
---

## About This Manual

This manual was automatically generated from the `--help` output of all installation scripts.

**To regenerate this manual:**
```bash
cd .devcontainer/manage
./generate-manual.sh
```

**To see help for a specific script:**
```bash
cd .devcontainer/additions
./install-<name>.sh --help
```

For more information about the devcontainer toolbox, see the main README.

